import React from 'react';
import PropTypes from 'prop-types';

const GrabbableBlock = (props) => (
  <div>
        GrabbableBlock
  </div>
);

GrabbableBlock.propTypes = {

};

export default GrabbableBlock;
