import { default as brands } from './brands';
import { default as funnel } from './funnel';
import * as common from './common';

export const brandsTypes = brands;
export const funnelTypes = funnel;
export { common };
